import React from 'react'

function PopupCrear(props){
    return (props.trigger) ? (
        <div>
            <div>
                
            </div>
        </div>
    ) : ""
}

export default PopupCrear;